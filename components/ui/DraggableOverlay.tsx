import React from 'react';

export default function DraggableOverlay({ id }: { id: string }) {
	return (
		<div className='size-32  bg-gray-700 shadow-sm border rounded-md p-3 flex flex-col justify-between'>
			DraggableOverlay
		</div>
	);
}
